import React from "react";
import { NavbarContainer } from "./header.styles";

const NavBar = () => {
    return (
        <NavbarContainer>
        <h1>My React App</h1>
        </NavbarContainer>
    );
    };

export default NavBar;