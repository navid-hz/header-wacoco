import React from "react";
import { NavbarContainer } from "./navbar.styles";

const Navbar = () => {
    return (
        <NavbarContainer>
        <h1>My React App</h1>
        </NavbarContainer>
    );
    };

export default Navbar;